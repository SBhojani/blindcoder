[package]
name = "alias"
version = "0.0.0"
edition = "2021"
license = "Apache-2.0"
description = "blindcoder aliasing: random stored tokens that mask model identity, behind a reveal gate."

[dependencies]
rand = "0.8"
